{
  "name": "unity-a-live-group-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for UNITY A LIVE GROUP Digital ID and Ganpati Gallery System",
  "scripts": {
    "dev": "node dev-server.js",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply unity-a-live-group-db --local",
    "migrate:remote": "wrangler d1 migrations apply unity-a-live-group-db --remote"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.5.0",
    "mongodb": "^6.8.0"
  },
  "devDependencies": {
    "wrangler": "^3.72.0"
  }
}
